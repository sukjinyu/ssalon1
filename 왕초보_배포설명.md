# 왕초보용 Cloudflare 배포 설명

명령어를 전부 외울 필요 없습니다. 이 폴더에 있는 `START_DEPLOY.cmd`를 더블클릭하면 됩니다.

## 제일 쉬운 방법

`START_DEPLOY.cmd` 파일을 더블클릭하세요. 새 검은 창이 열리고, 오류가 나도 창이 바로 닫히지 않게 해둔 가장 쉬운 방법입니다.

처음 실행했을 때 `Node.js was not found`가 나오면 https://nodejs.org 에서 무료 LTS 버전을 설치한 뒤 다시 `START_DEPLOY.cmd`를 더블클릭하세요.

## 터미널로 실행하는 방법

1. `royal-salon` 폴더를 엽니다.
2. 빈 공간에서 `Shift + 마우스 오른쪽 클릭`을 누릅니다.
3. `터미널에서 열기` 또는 `PowerShell 창 열기`를 누릅니다.
4. 아래 한 줄만 붙여넣고 Enter를 누릅니다.

```powershell
.\START_DEPLOY.cmd
```

그 다음부터는 화면에 나오는 설명을 따라가면 됩니다.

## 중간에 딱 두 번만 직접 하면 됩니다

### 1. Cloudflare 로그인

도우미가 브라우저를 열면 Cloudflare에 로그인하고 `Allow` 또는 `허용`을 누르면 됩니다.

### 2. database_id 붙여넣기

도우미가 D1 데이터베이스를 만들면 화면에 `database_id`가 들어간 줄이 나옵니다.

```toml
database_id = "12345678-abcd-1234-abcd-1234567890ab"
```

따옴표 안쪽 값만 복사해서 도우미가 물어보는 곳에 붙여넣으면 됩니다.

## 배포가 끝나면

마지막에 이런 주소가 나옵니다.

```text
https://royal-salon.pages.dev
```

이 주소가 인터넷에 올라간 내 게임 주소입니다.

## 비용

Cloudflare Pages와 D1은 무료 플랜으로 시작할 수 있습니다. 개인 테스트와 작은 게임 저장 정도는 무료 범위에서 시작하기 좋습니다. 사용량이 아주 커지면 Cloudflare가 안내를 띄울 수 있으니, 처음에는 무료 플랜으로만 진행하세요.

## 막히면 이 문장을 그대로 알려주세요

어느 단계에서 멈췄는지와 화면에 나온 오류 문장을 복사해서 저에게 보내주세요. 예를 들면 이렇게요.

```text
[4/5] Create database table에서 멈췄고, 이런 오류가 나왔어: ...
```


## PowerShell 보안 오류가 계속 뜰 때

`배포도우미_더블클릭.bat`도 막히면 `배포도우미_CMD전용.bat`를 더블클릭하세요.

이 파일은 PowerShell을 전혀 쓰지 않고 Windows 기본 명령 프롬프트(cmd)만 사용합니다.

순서:

1. `배포도우미_CMD전용.bat` 더블클릭
2. Cloudflare 로그인 창에서 `Allow / 허용`
3. 터미널에 나온 `database_id` 붙여넣기
4. 마지막에 나온 `https://...pages.dev` 주소 복사
